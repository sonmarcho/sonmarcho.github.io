# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# gem "rails"
gem "jekyll"
gem "minima"

# Easily manage the assets (images...) used in the posts
group :jekyll_plugins do
  gem 'jekyll-postfiles'
end

gem "webrick", "~> 1.7"
